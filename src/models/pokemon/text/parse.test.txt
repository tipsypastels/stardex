# Single name

Bulbasaur

==>

Program(Listing(Name))

# Single non-identifier name

Mr. Mime

==>

Program(Listing(Name))

# Multiple names

Foo
Bar

==>

Program(Listing(Name), Newline, Listing(Name))

# Specs

Foo (Fire)
Bar (Fire/Flying)
Baz (Form Name:Fire/Flying)

==>

Program(
  Listing(Name, Spec(TypeList)), 
  Newline, 
  Listing(Name, Spec(TypeList)), 
  Newline,
  Listing(Name, Spec(FormName, TypeList))
)

# Comments

Foo
# a comment
Bar @x

Baz

==>

Program(
  Listing(Name),
  Newline,
  Comment,
  Newline,
  Listing(Name, Modifier),
  Newline,
  Newline,
  Listing(Name)
)